<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Stemadvies.Views.HomePage"
             xmlns:vm="clr-namespace:Stemadvies.ViewModels"
             Title="{Binding Title}">
    
    <ContentPage.BindingContext>
        <vm:HomeViewModel />
    </ContentPage.BindingContext>
    
    <ContentPage.Content>
        <StackLayout>
            <Image Source="stemadvieslogo.png" HorizontalOptions="FillAndExpand" />
            <Label Text="Welkom bij stemadvies." HorizontalOptions="Center"/>
            <Label Text="In deze app kan je een aantal vragen beantwoorden om een advies te krijgen op wie je kan stemmen." HorizontalOptions="Start" HorizontalTextAlignment="Center"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>